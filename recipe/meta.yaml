{% set name = "tsid" %}
{% set version = "1.10.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/stack-of-tasks/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: c31f3c61a1a40c9d6e2411ef224da3db51c8a1fdb185425fc6ce84e06fe33862

build:
  number: 0
  skip: true  # [win]
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x.x') }}

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
    - cmake
    - ninja
  host:
    - python
    - eigenpy
    - libboost-python-devel
    - eigen
    - pinocchio
    - eiquadprog
    - proxsuite
    - urdfdom
    - numpy
  run:
    - python
test:
  requires:
    - pkg-config
  commands:
    - pkg-config --modversion {{ name }}
  imports:
    - tsid

about:
  home: http://github.com/stack-of-tasks/tsid
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE
  summary: TSID is a C++ library for optimization-based inverse-dynamics control based on the rigid multi-body dynamics library Pinocchio.
  dev_url: https://github.com/stack-of-tasks/tsid

extra:
  recipe-maintainers:
    - jorisv
    - EtienneAr
    - jcarpent
